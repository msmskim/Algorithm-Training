# 초기 값 지정
total = 0

for i in range(1, 1000):
    if i % 3 == 0 or i % 5 == 0:
        total += i
        
print(total)


# 한줄로 풀기
sum([x for x in range(1, 1000) if i % 3 == 0 or i % 5 == 0 ])

느낀점
1. 참 많은 문제를 쉽게 해결할 수 있다.
2. 내가 배운 내용 중에서 응용할 수 있는건 해야겠다.
3. 기본을 놓치지 않으면서 효율적인 코딩을 해야겠다.
